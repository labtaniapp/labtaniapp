

import 'package:flutter/cupertino.dart';

class doctordocument extends StatelessWidget {
  const doctordocument({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
